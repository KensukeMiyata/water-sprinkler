import FaceMask from './FaceMask';

const Masked = async () => {
  return (
    <div className="m-4">
      {/* <FaceMask /> */}
      <button type="button">画像を削除</button>
      <button type="button">画像を検出</button>
    </div>
  );
};

export default Masked;